export type UserData = {
    activatedAt: string,
    activationSource: string,
    blockedAt?: string,
    createdAt: string,
    email: string,
    id: number,
    login: string,
    requiresPasswordChange: boolean,
    uuid: string,
}